import {randomUUID} from 'node:crypto';
import {requireUser} from '@/lib/auth';
import {digest,encrypt} from '@/lib/security';
import {sql} from '@/lib/db';
import {providerFetch} from '@/lib/providers';
export async function GET(req:Request){try{const user=await requireUser();const q=new URL(req.url).searchParams;if(!q.get('code')||!q.get('state'))throw new Error('Missing code');const state=(await sql("DELETE FROM oauth_states WHERE state_hash=$1 AND user_id=$2 AND provider='instagram' AND expires_at>now() RETURNING workspace_id",[digest(q.get('state')!),user.id])).rows[0];if(!state)throw new Error('Invalid state');const form=new URLSearchParams({client_id:process.env.META_APP_ID!,client_secret:process.env.META_APP_SECRET!,grant_type:'authorization_code',redirect_uri:process.env.APP_URL+'/api/connect/instagram/callback',code:q.get('code')!});const short=await providerFetch('https://api.instagram.com/oauth/access_token',{method:'POST',body:form});const longParams=new URLSearchParams({grant_type:'ig_exchange_token',client_secret:process.env.META_APP_SECRET!,access_token:short.access_token});const long=await providerFetch('https://graph.instagram.com/access_token?'+longParams);const profile=await providerFetch('https://graph.instagram.com/me?fields=user_id,username',{headers:{Authorization:`Bearer ${long.access_token}`}});const external=String(profile.user_id||short.user_id);const existing=(await sql("SELECT workspace_id FROM connections WHERE provider='instagram' AND external_id=$1",[external])).rows[0];if(existing&&existing.workspace_id!==state.workspace_id)throw new Error('Account linked to another workspace');await sql("INSERT INTO connections(id,workspace_id,provider,external_id,name,secret,expires_at) VALUES($1,$2,'instagram',$3,$4,$5,now()+$6*interval '1 second') ON CONFLICT(provider,external_id) DO UPDATE SET name=excluded.name,secret=excluded.secret,expires_at=excluded.expires_at WHERE connections.workspace_id=excluded.workspace_id",[randomUUID(),state.workspace_id,external,profile.username||external,encrypt(long.access_token),long.expires_in||5184000]);if(process.env.META_API_VERSION)await providerFetch(`https://graph.instagram.com/${process.env.META_API_VERSION}/${external}/subscribed_apps`,{method:'POST',headers:{Authorization:`Bearer ${long.access_token}`,'Content-Type':'application/json'},body:JSON.stringify({subscribed_fields:'comments,messages'})});return Response.redirect(new URL('/app/integrations',process.env.APP_URL));}catch(e){console.error('instagram_connect_failed',e instanceof Error?e.name:'error');return Response.json({error:'تعذر إكمال الربط. راجع صلاحيات Meta وعناوين إعادة التوجيه ثم أعد المحاولة.'},{status:400});}}
